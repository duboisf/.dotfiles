if (( $# != 1 )); then
    echo "Usage: $0 <GITHUB SEARCH QUERY>"
    return 1
fi

local search_query="$(echo $1 | tr ' ' '+')"

gh api \
  -H "Accept: application/vnd.github+json" \
  -H "X-GitHub-Api-Version: 2022-11-28" \
  '/search/code?q='"$search_query"
